# Roadmap / Feature Pipeline (sorted by complexity ↑)

## 1. Непрозрачный текст в пиллах (easy)
- [ ] Убедиться что fillRect(0xFFFF) + текст(0x0000) не просвечивает карту
- [ ] Проверить selective composite — все ли пиксели тултипа перекрывают фон
- [ ] Убрать артефакты прозрачности по краям

## 2. Асинхронизация всего неинтерфейсного кода (easy-med) ✅
- [x] Проверить все модули на блокирующие вызовы в loop():
  - [x] `checkFetchedData()` — флаг + memcpy, heavy renderStatic раз в 3с
  - [x] `touch.take()` — очередь FreeRTOS
  - [x] render — ~16ms норм
- [x] Если остались блокирующие вызовы — вынести в task на core 0
  - HTTP fetch уже в фоне
  - WiFi уже неблокирующий
  - I2C touch уже в фоне

## 3. Значки типов транспорта (med)
- [ ] Определить тип из line prefix (U/S/M/Tram/Bus)
- [ ] Маленькие иконки (6-8px): bus-прямоугольник, tram-овал, S-Bahn-круг, U-Bahn-квадрат
- [ ] Рисовать рядом с нодой или в пилле перед названием линии

## 4. Настоящий бэкенд (med-hard)
- [ ] Включить реальный VBB API, DEMO_MODE=0
- [ ] Кэширование + fallback на демо при ошибке
- [ ] Проверить тайминги HTTP на ESP32

## 5. Плавные линии Безье (hard)
- [ ] Заменить прямые отрезки на квадратичные/кубические кривые Безье
- [ ] Контрольные точки через середину между нодами
- [ ] Рисование по параметру t (0..1)
- [ ] Проверить пересечения на сложных узлах
